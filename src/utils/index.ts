export const grades = ["A", "B", "C", "D", "F"];
export const subjects = ["English", "Math", "Science", "History"];
export type ISummaryData = {
  topGrade?: string;
  minGrade?: string;
  maxPass?: string;
  maxFail?: string;
};
